{% extends 'base.html.twig' %}
{% block title %}Catalog{% endblock %}
{% block body %}
    <ul>
    {% for category in categories %}
        <li><h3><a href="{{ path('show_category_product', {'categoryName':category.name }) }}">{{ category.name }}</a></h3></li>
    {% endfor %}
    </ul>
{% endblock %}
